%tr
  %td= grocery.name
  %td= grocery.category
  %td.action= link_to 'Edit', edit_grocery_path(grocery.id)
  %td.action= link_to 'Destroy', grocery_path(grocery.id), method: 'delete', data: { confirm: 'Are you sure?' }
